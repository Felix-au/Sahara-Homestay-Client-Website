{
  "name": "sahara-homestay-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "install-all": "npm install --prefix client && npm install --prefix server",
    "client-dev": "npm start --prefix client",
    "server-dev": "npm start --prefix server"
  }
}
